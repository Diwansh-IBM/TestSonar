public with sharing class ValidateSonarLint {
    public validateSonarLint() {
        list<Opportunities> oppList = new list<Opportunities>();
        For(Opportunties opp : oppList)
    list<Account> a =new list<Account>([select name from account]);    
}
}
